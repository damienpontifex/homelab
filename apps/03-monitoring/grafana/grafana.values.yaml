datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      # https://grafana.com/docs/grafana/latest/datasources/loki/
      - name: Loki
        type: loki
        access: proxy
        url: http://loki.monitoring.svc.cluster.local:3100
        isDefault: false
      # https://grafana.com/docs/grafana/latest/datasources/prometheus/
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://prometheus-server.monitoring.svc.cluster.local
        isDefault: true
      # https://grafana.com/docs/grafana/latest/datasources/tempo/
      - name: Tempo
        type: tempo
        access: proxy
        url: http://tempo.monitoring.svc.cluster.local:3200
        isDefault: false
      - name: Strava
        type: grafana-strava-datasource
        access: proxy
        isDefault: false
        jsonData:
          clientID: 102977
        secureJsonData:
          clientSecret: ${STRAVA_CLIENT_SECRET}

plugins:
  # https://grafana.com/grafana/plugins/grafana-strava-datasource/
  - grafana-strava-datasource
        

# Ref: https://grafana.com/docs/grafana/latest/setup-grafana/configure-grafana/
grafana.ini:
  server:
    root_url: http://localhost
  # ========================================================================
  # Logging
  # ========================================================================
  log:
    mode: console
    level: info
  log.console:
  format: json
  # ========================================================================
  # Authentication
  # https://grafana.com/docs/grafana/latest/setup-grafana/configure-access/configure-authentication/entraid/
  # ========================================================================
  auth.anonymous:
    enabled: true
    org_role: Admin

  feature_toggles:
    # Git sync https://grafana.com/docs/grafana/latest/as-code/observability-as-code/git-sync/git-sync-setup/set-up-before/
    provisioning: true
    # For strava plugin
    externalServiceAccounts: true

  panels:
    enable_alpha: true

service:
  type: LoadBalancer
  annotations:
    external-dns.alpha.kubernetes.io/hostname: grafana.pontifex.dev
    
# kubectl create secret generic grafana-secrets --namespace monitoring --from-literal=STRAVA_CLIENT_SECRET=$(pbpaste)
envFromSecret: grafana-secrets

persistence:
  enabled: true
securityContext:
  runAsUser: 472
  runAsGroup: 472
  fsGroup: 472

initChownData:
  enabled: false # Disable the container causing the error

serviceAccount:
  annotations:
    azure.workload.identity/client-id: ce700695-4e5c-4898-a6e9-4dc7bb00fbaa
    azure.workload.identity/tenant-id: ff2b9041-8733-4fbd-a4e6-23f30567c4a4

podLabels:
  azure.workload.identity/use: "true"

sidecar:
  dashboards:
    enabled: true
    searchNamespace: ALL
  datasources:
    enabled: false
    searchNamespace: ALL
