apiVersion: v1
kind: Service
metadata:
  name: home-assistant
  annotations:
    external-dns.alpha.kubernetes.io/hostname: hass.pontifex.dev
spec:
  type: LoadBalancer
  ports:
    - name: home-assistant
      protocol: TCP
      port: 443
      targetPort: 8123

