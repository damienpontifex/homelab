apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jellyfin-app-cert
  # annotations:
  #   cert-manager.io/issue-temporary-certificate: "true"
spec:
  secretName: jellyfin-app-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - media.pontifex.dev

