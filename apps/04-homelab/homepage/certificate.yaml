apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage-app-cert
spec:
  secretName: homepage-app-tls-secret
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
  - home.pontifex.dev

